int x,test=1;
    cin>>x;
    int a[x];
    arr(x)
    while(test)
    { test =0;
        for(int i=0; i<x-1; i++)
        {
            if(a[i]>a[i+1])
            {
                swap(a[i],a[i+1]);
                   test = 1;
            }
        }
    }
    arro(x)
